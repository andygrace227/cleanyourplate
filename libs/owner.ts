/*
User class for CleanYourPlate.
An interface that describes how shared resources in a household behave.
Contains functions for generating notifications to the user
*/

class User {
    isHouseholdOwner : boolean;

    displayName : string;

    email: string;
    
    phoneNumber: string;
}